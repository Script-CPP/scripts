#include <stdio.h>
#include <stdlib.h>

struct num {
	int x;
};

int main(){
	
	struct num* n;

	while(1){
	
		// allocate memory on the heap for a new struct
		n = (struct num*)malloc(sizeof(struct num));

	}

return 0;
}
